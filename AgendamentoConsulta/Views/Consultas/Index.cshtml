@model IEnumerable<AgendamentoConsulta.Models.Consulta>

@{
    ViewData["Title"] = "Consultas - Médico";
}

<h2 class="panel-title">@ViewData["Title"]</h2>

<p>
    <a asp-action="Create" class="btn btn-default btn-success">Marcar consulta</a>
</p>

<div class="row">
    <div col-md-6 class="col-md-6">
        <form class="form-horizontal" asp-action="ConsultaPorMedico">
            <fieldset>
                <legend>Pesquisa</legend>
                <div class="form-group">
                    <label for="nomeMedico" class="col-lg-2 control-label">Médico</label>
                    <div class="col-lg-10">
                        <input type="text" name="nomeMedico" class="form-control" />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-10 col-lg-offset-2">
                        <button type="submit" class="btn btn-primary">Filtrar</button>
                    </div>
                </div>
                <hr>
                <label class="control-label h4">Consultas pendentes</label>
                <div class="form-group">
                    <div class="col-lg-10 col-lg-offset-2">
                        <button type="submit" class="btn btn-primary" asp-action="ConsultasPendentes">Abrir</button>
                    </div>
                    </div>
            </fieldset>
        </form>
    </div>
</div>

<table class="table table-striped table-hover">
    <thead>
        <tr class="bg-dark text-light text-justify">
            <th>
                @Html.DisplayNameFor(model => model.Paciente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sintoma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diagnostico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataConsulta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ConsultaSituacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="success">
                <th>
                    @Html.DisplayFor(modelItem => item.Paciente.Nome)
                </th>
                <th>
                    @Html.DisplayFor(modelItem => item.Medico.Nome)
                </th>
                <td>
                    @Html.DisplayFor(modelItem => item.Sintoma)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Diagnostico)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataConsulta)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ConsultaSituacao.DescricaoSituacao)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-default btn-info">Editar</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-default btn-danger">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
